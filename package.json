{
  "name": "@prmichaelsen/youtube-mcp-server",
  "version": "1.0.0",
  "description": "MCP server wrapping the YouTube Data API v3 for AI agents, mcp-auth enabled",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "node esbuild.build.js",
    "watch": "node esbuild.watch.js",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "jest",
    "type-check": "tsc --noEmit",
    "deploy": "gcloud builds submit --config=cloudbuild.yaml --substitutions=SHORT_SHA=$(git rev-parse --short HEAD) .",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@prmichaelsen/mcp-auth": "^7.4.2",
    "@prmichaelsen/youtube-mcp": "^1.1.1",
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.24.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
